import { StyleSheet, Text, View } from 'react-native';

export default function AnalyticsAndReports() {
    return (
      <View style={styles.container}> 
        <Text>AnalyticsAndReports</Text>
      </View>
    );
}

const styles = StyleSheet.create({
    container: {
        flex: 1,
        alignItems: 'center',
        justifyContent: 'center',
    },
});